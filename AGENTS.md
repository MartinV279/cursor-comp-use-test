# AGENTS.md

## Cursor Cloud specific instructions

This repository (`cursor-comp-use-test`) is currently an empty skeleton with only a `README.md`. There are no services, dependencies, build steps, or tests to run.

- **No package manager lockfiles** exist — no `npm install`, `pip install`, etc. needed.
- **No services** to start or test.
- **No lint, test, or build commands** are configured.

When code is added to this repository, update this section with the relevant development workflow instructions (how to install deps, lint, test, build, and run).
